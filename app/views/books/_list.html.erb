<!--<p>Find me in app/views/books/_list.html.erb</p>
<p>部分共通コード(left_side)/app/views/books/_list.html.erb</p>-->
<div class="col-lg-3">
  <div class="container col-lg-12">
    <h2>User info</h2>
    <%= attachment_image_tag current_user, :profile_image, :fill, 100, 100, class: "profile-thumb", fallback: "no_image.jpg"%>
    <table class="table">
      <tbody>
        <tr></tr>
        <tr>
          <th>name</th>
          <th><%= current_user.name %></th>
        </tr>
        <tr>
          <th>introduction</th>
          <th><%= current_user.introduction %></th>
        </tr>
      </tbody>

      <!--<button type="button" class="btn btn-default col-lg-12">
    <span class="glyphicon glyphicon-wrench" aria-hidden="true"></span>
  </button>-->
    </table>

    <div class="field row">
      <%= link_to edit_user_path(current_user) do %>
        <i class="btn btn-default  glyphicon glyphicon-wrench col-lg-12"></i>
        <% end %>
    </div>
    <!--New book投稿フォーム-->
    <!--<h2>New book</h2>

<p>Title</p>
<input class="input col-lg-12">
<p>Opinion</p>
<textarea class="textarea  col-lg-12"></textarea>
<button type="button" class="btn btn-primary col-lg-12">Create book</button>
</div>
</div>-->



    <h2>New book</h2>
    <%= form_for(@book) do |f| %>
        <% if @book.errors.any? %>
  <div id="error_explanation" class="col-lg-12 alert alert-danger">
    <ul>
      <% @book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
      <div class="field row">
        <label>Title</label>
        <%= f.text_field :title, class: " col-lg-12"%>
      </div>

      <div class="field row">
        <label>Opinion</label>
        <%= f.text_area :opinion, class: " col-lg-12" %>
      </div>

      <div class="actions row">
       <%= f.submit "Create Book", class:"btn btn btn-primary col-lg-12" %>
      </div>
      <% end %>
  </div>
</div>